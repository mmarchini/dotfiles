# dunst

`dunst` notifications config files

## Install

```bash
# Teiler setup (screenshots and screen recording)
sudo apt install dunst
mkdir -p ~/.config/dunst/
ln -s $(pwd)/dunstrc ~/.config/dunst/dunstrc
```
