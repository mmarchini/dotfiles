# Vim dotfiles

## Install

```bash
sudo apt install vim curl
# WARNING: This will override your .vimrc. Make a backup first
ln -s $(pwd)/.vimrc ~/.vimrc
# Open vim the first time to intall vim-plug + plugins
vim
```
