# i3wm

`i3` window manager config files

## Install

```bash
sudo apt install i3-wm i3status py3status
mkdir -p ~/.config/i3/
ln -s $(pwd)/.i3status.conf ~/.i3status.conf
ln -s $(pwd)/config ~/.config/i3/config
```
